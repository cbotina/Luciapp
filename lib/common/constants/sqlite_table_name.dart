import 'package:flutter/foundation.dart' show immutable;

@immutable
class SQLiteTableName {
  static const themeSettings = 'theme_settings';
  static const fontSettings = 'font_settings';

  const SQLiteTableName._();
}
