import 'package:flutter/material.dart';

class CloudCourseColors {
  Color mainColor;
  Color shadowColor;
  Color highlightColor;

  CloudCourseColors({
    required this.mainColor,
    required this.shadowColor,
    required this.highlightColor,
  });
}
