import 'package:flutter/foundation.dart' show immutable, ValueKey;

@immutable
class Keys {
  static const darkModeIconButton = ValueKey('dark-mode-icon-button');
  static const darkModeButton = ValueKey('dark-mode-button');
  static const highContrastButton = ValueKey('hc-button');
}
